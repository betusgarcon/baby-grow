# Baby Growth AI 基础设施与落地方案

## 1. 目标与判断

结合当前 Figma 页面语义、你补充的业务想法，以及现有后端架构文档，我建议这套系统采用：

- `本地优先` 的 AI 基础设施
- `规则 + 检索 + 模型生成` 的混合决策链路
- `Java 主业务后端 + Python AI 编排层 + 本地模型网关` 的分层架构
- `结构化知识库 + RAG + 审计日志` 的可追溯方案
- `本地私有化为主，云端/API 为辅` 的兜底策略

核心原因不是“为了上 AI 而上 AI”，而是因为你的场景天然有三类不同能力：

1. `高确定性判断`
   - 例如月龄是否适配某种食材
   - 例如过敏原是否冲突
   - 例如牙齿与质地是否匹配

2. `语义理解`
   - 例如用户输入“今天宝宝学会了爬行”
   - 需要判断它是普通记录、成长事件，还是里程碑
   - 还要抽取标签、主题、建议动作

3. `个性化生成`
   - 例如一周或一月报告建议
   - 例如基于月龄、饮食偏好、过敏原、牙齿发育、近期记录生成喂养建议

这三类能力不应该都交给一个大模型直接输出，而应该分层处理。

---

## 2. 从页面反推的 AI 场景

从已有设计语义和后端文档看，这个产品至少包含以下与 AI 强相关的页面或功能：

- `Journey / 时间线`
  - 成长记录输入
  - 自动分类
  - 标签补全
  - 里程碑识别

- `Analysis / 分析页`
  - 成长数据聚合
  - 睡眠、饮食、情绪、发育摘要
  - 周报、月报 AI 建议

- `Baby Profile / 宝宝画像`
  - 月龄
  - 过敏原
  - 饮食偏好
  - 牙齿发育
  - 辅食阶段

- `AI 配餐 / 辅食建议`
  - 根据月龄、画像、禁忌与知识库生成建议

- `Milestone / 仪式感里程碑`
  - 高置信度成长节点识别
  - 低置信度二次确认

所以这不是一个单点的“聊天式 AI”产品，而是一个 `嵌入业务流程的 AI Copilot`。

---

## 3. 一句话拍板

如果现在让我直接拍板方案，我会选：

- `Java` 做核心事务和业务域
- `Python` 做 AI 编排、RAG、知识检索和模型路由
- `Ollama` 作为本地 LLM 统一接入层
- `PostgreSQL + pgvector` 作为本地知识库和向量检索层
- `MySQL + Redis + RabbitMQ + MinIO` 作为业务基础设施
- `规则引擎 + 结构化过滤 + 混合检索 + LLM 结构化输出` 作为 AI 决策主链路

这条路线兼顾：

- 可快速启动
- 本地隐私可控
- 可解释
- 后续可接云模型
- 能较好抑制幻觉

---

## 4. 总体 AI 架构

### 4.1 架构分层

建议分成 6 层：

1. `业务接入层`
   - 小程序
   - Gateway/BFF
   - Core Service

2. `AI 编排层`
   - Prompt 模板
   - 模型路由
   - 检索编排
   - 输出解析
   - 置信度与兜底策略

3. `模型执行层`
   - 本地 LLM
   - Embedding 模型
   - 可选 Reranker 模型
   - 可选云模型/API

4. `知识与数据层`
   - 结构化知识库
   - 非结构化知识文档
   - 向量索引
   - 规则库

5. `异步任务层`
   - 事件分类任务
   - 周报/月报任务
   - 知识入库任务
   - 重试和死信处理

6. `审计与观测层`
   - 模型调用日志
   - Prompt 版本
   - 检索命中文档
   - 输出快照
   - 用户反馈闭环

### 4.2 拓扑建议

```text
微信小程序
   |
   v
Gateway / BFF
   |
   v
Core Service (Java)
   |
   +--> MySQL / Redis / MinIO
   |
   +--> RabbitMQ
           |
           +--> AI Service (Python / FastAPI)
                    |
                    +--> Rule Engine
                    +--> Prompt Registry
                    +--> Retrieval Pipeline
                    +--> Output Validator
                    |
                    +--> Ollama (本地模型)
                    +--> PostgreSQL + pgvector
                    +--> Optional Cloud Model Gateway
```

---

## 5. 为什么建议本地优先

你已经明确希望优先在本地建设 AI 基础设施，这个方向非常适合当前场景，主要有 5 个原因：

- `儿童和家庭数据敏感`
  - 成长记录、饮食习惯、健康偏好、过敏原都属于高敏感信息

- `可控性更强`
  - 模型、知识库、日志都掌握在本地

- `成本可预测`
  - 高频分类和摘要不持续消耗外部 API 费用

- `便于调试`
  - 本地可以快速反复试 Prompt、切模型、跑回归样本

- `比赛/作品展示友好`
  - 能体现完整私有化 AI 能力，而不只是 API 套壳

但本地优先不代表完全拒绝云端，建议是：

- `默认走本地`
- `复杂低频任务按策略允许云端兜底`
- `云端必须明确开关、脱敏、白名单和审计`

---

## 6. 推荐基础设施

### 6.1 必选组件

- `Ollama`
  - 作为本地模型统一入口
  - 优点是切换模型简单，调用方式统一

- `PostgreSQL + pgvector`
  - 兼顾结构化知识和向量检索
  - 很适合第一阶段知识库建设

- `RabbitMQ`
  - 承接异步 AI 任务
  - 尤其适合成长记录分类和定期报告生成

- `MySQL`
  - 存业务主数据和 AI 结果快照

- `Redis`
  - 缓存热点画像、知识片段、模型结果
  - 做幂等、限流和短期状态管理

- `MinIO`
  - 存图像、附件、知识库原始文件、导入包

### 6.2 AI 服务框架建议

- `FastAPI`
  - 轻量、开发快、适合结构化接口

- `Pydantic`
  - 用于请求响应和模型输出校验

- `SQLAlchemy`
  - 管理 AI 任务记录、知识库元数据、审计日志

- `Celery` 或轻量任务队列
  - 如果异步任务后续增多，再补 Worker

### 6.3 在 Mac 上的建议

如果本地硬件主要是 Apple Silicon Mac，建议：

- 先以 `单机 + Docker Compose` 为主
- `Ollama` 单独部署
- 高频任务优先使用 `7B` 左右模型
- 周报/月报这类低频高价值任务再考虑更强模型
- Embedding 模型和生成模型分开，不要混用

不建议第一版就追求：

- 多模型并发常驻
- 复杂 Agent 框架
- 超重的多阶段自动规划系统

第一版最重要的是稳定和可解释。

---

## 7. 模型分层建议

### 7.1 不同任务用不同模型

建议至少分 3 类模型：

1. `分类/抽取模型`
   - 用于成长文本分类、标签抽取、里程碑候选识别
   - 目标是低延迟、高稳定、结构化输出好

2. `生成模型`
   - 用于配餐建议、周报/月报建议、自然语言摘要
   - 目标是上下文理解强、表达稳定

3. `Embedding 模型`
   - 用于知识向量化和检索
   - 目标是中文召回效果稳定

### 7.2 推荐的模型策略

可以按以下思路选型：

- `分类/抽取`
  - 优先中文或中英双语能力较好的指令模型
  - 示例方向：`Qwen 7B 级别`、轻量指令模型

- `生成`
  - 选择比分类模型更强的本地模型
  - 示例方向：`Qwen 14B 级别` 或同档位模型

- `Embedding`
  - 选择中文检索表现好的多语向量模型
  - 示例方向：`bge-m3` 这类多语 embedding

- `Reranker`
  - 第二阶段可选
  - 用于提升知识片段重排精度

### 7.3 模型网关必须做的事

不要让业务直接调用模型，建议通过 `Model Gateway` 做统一封装：

- 统一鉴权
- 统一超时控制
- 统一日志
- 统一重试
- 统一 Prompt 模板版本
- 统一 JSON Schema 输出校验
- 统一模型路由策略

---

## 8. AI 能力分层设计

### 8.1 成长文本识别

用户输入示例：

> 今天宝宝学会了爬行

建议链路：

1. `Core Service` 落库原始文本
2. 发布 `growth.event.created`
3. `AI Service` 读取宝宝画像与事件文本
4. 规则层先做粗分类
5. 分类模型做事件识别和标签抽取
6. 输出结构化结果
7. Core 回写 AI 结果
8. 高置信度命中里程碑时生成 `MilestoneEvent`
9. 低置信度则进入待确认状态

输出建议结构：

```json
{
  "eventType": "GROWTH",
  "subType": "MOTOR_DEVELOPMENT",
  "isMilestone": true,
  "milestoneCode": "CRAWLING",
  "tags": ["爬行", "大运动", "发育进步"],
  "confidence": 0.95,
  "needUserConfirm": false,
  "reasoningBasis": [
    "文本包含学会了爬行",
    "命中大运动里程碑语义"
  ]
}
```

### 8.2 配餐与辅食建议

这是最典型的 `规则 + RAG + 生成` 场景。

建议分三层：

1. `硬规则层`
   - 月龄边界
   - 过敏原排除
   - 医嘱禁忌
   - 牙齿和咀嚼能力
   - 食材引入顺序

2. `知识检索层`
   - 月龄适配食谱
   - 辅食指南
   - 食材注意事项
   - 过敏原文档
   - 烹饪质地建议

3. `生成层`
   - 根据画像、规则过滤结果、近期饮食记录、知识片段生成当日或当周建议

模型在这里的职责不是“决定可不可以吃”，而是：

- 组织语言
- 个性化排序
- 给出更自然的解释
- 生成替代方案

### 8.3 周报/月报建议

这类任务适合做成异步离线流程：

1. 定时聚合一周或一月数据
2. 生成标准统计摘要
3. 检索同月龄知识和关注点
4. 生成结构化建议
5. 输出给前端展示

建议输出也分层：

- `事实层`
  - 本周新增记录数量
  - 饮食偏好变化
  - 体重或身高趋势

- `解释层`
  - 当前阶段关注重点

- `建议层`
  - 饮食建议
  - 作息建议
  - 发育观察建议

- `提示层`
  - 必须声明非医疗结论

---

## 9. RAG 设计建议

### 9.1 知识库一定要分层

你提到要搜集餐食知识库、采集不同月龄知识数据、构建专业知识 RAG。这里不要只建一个“大文档向量库”，建议拆成 4 层：

1. `结构化知识层`
   - 月龄阶段
   - 食材
   - 过敏原
   - 牙齿阶段
   - 质地等级
   - 食谱适配规则

2. `半结构化知识层`
   - FAQ
   - 医学/营养指南摘要
   - 引入建议
   - 风险说明

3. `非结构化文档层`
   - 指南全文
   - 长篇育儿资料
   - 原始采集文档

4. `业务反馈层`
   - 用户确认结果
   - 被采纳建议
   - 用户手工纠错

### 9.2 为什么不能只做向量检索

如果只做向量检索，很容易出现：

- 月龄不准
- 召回片段有语义相关但规则冲突
- 明明过敏却仍被推荐
- 文档说法覆盖不完整

所以检索流程建议是：

```text
用户/任务请求
  -> 画像解析
  -> 结构化过滤
  -> 向量召回
  -> 关键词/标签补召回
  -> 可选重排
  -> Prompt 拼装
  -> JSON 输出校验
```

### 9.3 RAG 召回策略

推荐采用 `混合检索`：

- `结构化过滤`
  - 先按月龄、禁忌、牙齿阶段过滤

- `向量召回`
  - 召回语义相关知识

- `关键词召回`
  - 对食材名、过敏原、症状词做精确补召回

- `可选重排`
  - 对前 20 条做 rerank

### 9.4 知识切片建议

不要一股脑把长文档按固定长度切片，建议：

- 指南类文档按章节切
- FAQ 一问一答切
- 食谱一条一条切
- 风险提示按规则颗粒度切
- 每个 chunk 强制带 `metadata`

推荐 metadata：

- `age_min_month`
- `age_max_month`
- `food_stage`
- `texture_level`
- `allergen`
- `source`
- `authority_level`
- `version`
- `updated_at`

---

## 10. 如何降低幻觉和知识覆盖不全

这是你提得非常对的重点。我的建议是把“降低幻觉”拆成 7 个机制，而不是只靠 Prompt：

### 10.1 硬规则前置

所有安全边界必须先经过规则判断，尤其是：

- 月龄
- 过敏原
- 咀嚼能力
- 禁忌食材
- 医疗风险提示

### 10.2 限制模型回答空间

Prompt 不要让模型自由发挥，应该要求：

- 只能基于给定知识片段回答
- 必须返回结构化 JSON
- 必须输出引用的知识 ID
- 不确定时返回 `INSUFFICIENT_EVIDENCE`

### 10.3 强制输出校验

对模型输出做 JSON Schema 校验，失败就：

- 自动重试一次
- 再失败走模板兜底

### 10.4 引用与证据

每条建议都应带：

- `sourceRefs`
- `confidence`
- `reasonCodes`

前端不一定全部展示，但后端必须保留。

### 10.5 知识覆盖率监控

不是所有问题都要立即回答。建议统计：

- 哪些月龄知识不足
- 哪些食材缺少说明
- 哪些过敏组合没有足够覆盖
- 哪些问题频繁命中低置信度

### 10.6 低置信度降级

当出现以下情况时，不直接生成强建议：

- 召回片段过少
- 片段来源冲突
- 输出不稳定
- 画像缺失严重

降级为：

- 模板化保守建议
- 提示补充画像信息
- 仅返回明确可确认的事实

### 10.7 人工反馈闭环

建议在产品里给用户留轻量反馈：

- 这个建议有帮助吗
- 这个分类是否正确
- 是否需要纠正标签

这些反馈会成为后续：

- Prompt 优化依据
- 规则修正依据
- 知识补充依据

---

## 11. 知识采集与治理路线

### 11.1 知识来源建议

建议优先采集以下类型内容：

- 权威喂养指南
- 不同月龄辅食建议
- 食材引入顺序
- 常见过敏原说明
- 牙齿发育与食物质地建议
- 育儿 FAQ
- 家庭常见误区说明

### 11.2 入库前必须做治理

不要直接把抓来的文本塞进向量库。建议先经过：

1. `来源标记`
2. `版本管理`
3. `去重`
4. `清洗`
5. `结构化抽取`
6. `标签化`
7. `切片`
8. `向量化`

### 11.3 知识对象模型建议

至少定义这些实体：

- `KnowledgeDocument`
- `KnowledgeChunk`
- `Recipe`
- `Ingredient`
- `AllergenRule`
- `AgeStageRule`
- `DentalStageRule`
- `FoodTextureRule`
- `KnowledgeSource`
- `KnowledgeVersion`

### 11.4 知识更新策略

建议做两种更新：

- `全量基线包`
  - 初始化导入
  - 比赛/demo 阶段够用

- `增量补丁包`
  - 新增或修正文档
  - 重建对应 chunk 和 embedding

---

## 12. 模型交互层设计

### 12.1 不建议业务直接写 Prompt

建议单独做 `Prompt Registry`：

- `event_classification_v1`
- `meal_recommendation_v1`
- `weekly_report_v1`
- `monthly_report_v1`

每个 Prompt 都保留：

- 模板内容
- 输入参数定义
- 输出 Schema
- 适用模型
- 版本号

### 12.2 Model Gateway 的职责

建议提供统一接口：

- `classifyGrowthEvent()`
- `generateMealRecommendation()`
- `generatePeriodicReport()`
- `embedDocuments()`

同时网关内部支持：

- 路由到本地模型
- 路由到云模型
- 超时与熔断
- 限流
- 降级
- 审计

### 12.3 典型调用协议

AI 服务对外不要暴露“聊天式自由接口”，而应该暴露任务接口：

- `POST /ai/tasks/event-classification`
- `POST /ai/tasks/meal-recommendation`
- `POST /ai/tasks/periodic-report`
- `POST /ai/knowledge/index`

这样更利于：

- 控制输入边界
- 做结构化输出
- 做幂等和审计

---

## 13. 与后端的接口协作建议

### 13.1 Java 与 Python 的边界

建议边界如下：

- `Java Core Service`
  - 负责主业务流程、事务落库、权限、状态机、审计入口

- `Python AI Service`
  - 负责模型调用、检索、知识编排、输出校验、AI 日志

### 13.2 推荐事件流

#### 成长记录分类

```text
用户提交文本
  -> Core 落库 growth_events
  -> MQ 发布 growth.event.created
  -> AI Service 消费
  -> 规则初筛 + 模型分类
  -> 结果写回 growth_event_ai_results
  -> 命中里程碑则生成 milestone
```

#### 配餐建议

```text
用户请求建议
  -> Core 读取宝宝画像
  -> 生成 AI 任务
  -> AI Service 执行规则过滤 + 检索 + 生成
  -> 保存 recommendation record
  -> Core 返回最终建议或查询结果
```

#### 周报/月报

```text
定时任务触发
  -> 聚合成长数据与饮食数据
  -> 检索同阶段知识
  -> 生成结构化报告
  -> 落库存档
  -> 前端展示
```

---

## 14. 数据表补充建议

如果你要补 AI 相关表，建议最少包含：

### 14.1 业务侧

- `growth_event_ai_results`
- `meal_recommendation_records`
- `analysis_snapshots`
- `milestone_events`

### 14.2 AI 任务侧

- `ai_task_records`
- `ai_decision_logs`
- `prompt_versions`
- `model_configs`

### 14.3 知识侧

- `knowledge_documents`
- `knowledge_chunks`
- `recipes`
- `recipe_rules`
- `knowledge_ingestion_jobs`

### 14.4 用户反馈侧

- `ai_feedback_records`
  - 记录分类纠正、建议采纳情况、低质量反馈

---

## 15. 云端兜底策略

虽然优先本地，但建议架构上预留云模型或外部平台接入能力，比如：

- 官方大模型 API
- 私有云模型服务
- 类似 FastGPT 的工作流平台

### 15.1 哪些场景适合云端兜底

- 本地模型超时
- 周报/月报需要更强生成质量
- 某些复杂长文本总结
- 后续需要多轮对话助手

### 15.2 接入云端前必须满足

- 脱敏
- 开关控制
- 明确用户授权
- 数据审计
- 可回退到本地

### 15.3 推荐路由策略

```text
默认本地
  -> 本地成功则直接返回
  -> 本地超时/低质量/策略命中
  -> 可选云端兜底
  -> 全程记录 route=local/cloud
```

---

## 16. 分阶段实施路线

### Phase 0: 先定义标准

先不要急着上模型，先把基础定义好：

- 事件分类体系
- 里程碑枚举
- 标签体系
- 宝宝画像字段
- 食谱与规则字段
- 知识元数据标准
- AI 输出 JSON Schema

如果这一步没做好，后面模型再强也会混乱。

### Phase 1: 本地基础设施跑通

目标：

- Docker Compose 起服务
- Ollama 跑通本地模型
- PostgreSQL + pgvector 建好知识库
- FastAPI AI Service 能被 Core 调用

交付物：

- `compose.yaml`
- `ai-service`
- `model gateway`
- `knowledge schema`

### Phase 2: 成长文本分类闭环

目标：

- 用户文本 -> AI 分类 -> 标签 -> 里程碑候选 -> 时间线回写

优先做：

- 规则粗分类
- 分类模型结构化输出
- 低置信度待确认
- AI 决策日志

### Phase 3: 配餐 RAG 闭环

目标：

- 宝宝画像 + 月龄 + 过敏原 + 知识库 -> 生成每日/每周建议

优先做：

- 结构化规则库
- 食谱和辅食知识入库
- 混合检索
- 结构化建议返回

### Phase 4: 周报/月报智能建议

目标：

- 对周期数据做摘要和建议生成

重点做：

- 周期聚合器
- 事实层和建议层分离
- 引用知识片段
- 风险提示和非医疗声明

### Phase 5: 云端兜底与智能运维

目标：

- 根据质量、耗时和成本做模型路由
- 补充知识覆盖率分析
- 补充回归测试和 A/B 对比

---

## 17. 推荐的第一阶段最小闭环

如果你现在时间有限，我建议先只做下面 3 个闭环：

### 17.1 闭环一：成长记录智能分类

这是最能体现 AI 价值、又最容易快速完成的闭环：

- 输入自然语言
- 自动分类
- 自动打标签
- 自动识别里程碑候选

### 17.2 闭环二：宝宝画像驱动的配餐建议

这是最贴近产品价值的闭环：

- 读取宝宝月龄与画像
- 通过知识库与规则过滤
- 输出个性化辅食建议

### 17.3 闭环三：周报/月报建议

这是最适合展示“AI 总结能力”的闭环：

- 聚合成长数据
- 输出结构化报告
- 展示个性化建议

只要这 3 个闭环做扎实，你的项目已经非常完整。

---

## 18. 技术实现建议清单

### 18.1 后端接口

建议新增或明确以下接口：

- `POST /api/v1/events`
- `GET /api/v1/ai/event-results/{eventId}`
- `POST /api/v1/ai/events/{eventId}/reclassify`
- `POST /api/v1/ai/meal-recommendations`
- `GET /api/v1/ai/meal-recommendations/history`
- `POST /api/v1/ai/reports/weekly`
- `POST /api/v1/ai/reports/monthly`

### 18.2 AI 内部接口

- `POST /internal/ai/classify-event`
- `POST /internal/ai/generate-meal-plan`
- `POST /internal/ai/generate-periodic-report`
- `POST /internal/knowledge/index-documents`
- `POST /internal/knowledge/rebuild-embeddings`

### 18.3 建议先实现的模块

- `Prompt Registry`
- `Model Gateway`
- `Retrieval Service`
- `Rule Engine`
- `Decision Log Service`
- `Feedback Collector`

---

## 19. 风险点与规避建议

### 19.1 风险一：知识库没结构化，RAG 很快失控

规避：

- 先做知识对象建模
- 先做 metadata 标准
- 先做规则表

### 19.2 风险二：一开始就做大而全 Agent

规避：

- 第一版只做任务式 AI
- 不做自由对话 Agent
- 不做自动工具链规划

### 19.3 风险三：模型输出不可控

规避：

- 全部结构化 JSON 输出
- 增加输出校验
- 低置信度降级

### 19.4 风险四：本地机器资源被打爆

规避：

- 控制常驻模型数量
- 分类模型和生成模型分层调用
- 异步化低优先级任务

### 19.5 风险五：建议内容带医疗误导

规避：

- 规则层先兜底
- 输出添加非医疗声明
- 高风险内容禁止模型自由推断

---

## 20. 我对你项目的最终建议

### 20.1 架构选择

首选方案：

- `Java + Python` 双栈
- `本地 Ollama + pgvector`
- `规则 + RAG + LLM`

### 20.2 实施优先级

建议顺序：

1. 先定义分类体系和知识模型
2. 再拉起本地 AI 基础设施
3. 先做成长记录分类闭环
4. 再做配餐 RAG
5. 最后做周报/月报生成

### 20.3 适合比赛与长期演进

这套方案既适合：

- 比赛展示
- 作品集展示
- 后续继续迭代
- 私有化部署

也避免了“只有一个聊天框”的空泛 AI 设计。

---

## 21. 下一步我建议你确认的几个问题

为了把下一版方案收敛到更可执行，我建议你补充以下信息：

1. 你更偏向 `先快速可演示`，还是 `一期就想把工程结构定很稳`？
2. 你的本地硬件具体是 `哪款 Mac、多少内存`？
3. 第一阶段是否要求 `完全离线可运行`？
4. 你希望知识库更偏 `餐食/辅食`，还是同步覆盖 `睡眠/情绪/发育`？
5. 你后端更倾向 `Spring Boot + FastAPI`，还是希望我给你更轻量的单体方案？

如果你愿意，下一步我可以继续直接帮你补下面任意一项：

- `AI 服务目录结构设计`
- `知识库表结构 SQL 草案`
- `成长事件分类标签体系`
- `Meal Recommendation 的 Prompt 设计`
- `Docker Compose 落地文件`
- `Java/Python 服务接口协议`
